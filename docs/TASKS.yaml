## Task Backlog — IntentDOT
## Polkadot Solidity Hackathon 2026 | Deadline: Mar 20

tasks:
  - id: T001
    subject: "Scaffold project: Next.js + wagmi + Foundry monorepo"
    status: completed
    priority: P0
    estimate_hours: 3
    owner: claude
    blocked_by: []
    tags: [setup, must-have]
    notes: "Done. frontend/ (Next.js 14 + wagmi v2 + Tailwind) + contracts/ (Foundry 0.8.24)"

  - id: T002
    subject: "Write MockDEX.sol — Uniswap V2 style AMM (addLiquidity + swap)"
    status: completed
    priority: P1
    estimate_hours: 5
    owner: claude
    blocked_by: [T001]
    tags: [smart-contract, must-have]
    notes: "Done. Constant product AMM with 0.3% fee, getAmountOut, getPool, events"

  - id: T003
    subject: "Write IntentExecutor.sol — entry point that routes to MockDEX"
    status: completed
    priority: P1
    estimate_hours: 4
    owner: claude
    blocked_by: [T002]
    tags: [smart-contract, must-have]
    notes: "Done. executeSwap + previewSwap, reentrancy guard, IntentExecuted event"

  - id: T004
    subject: "Write MockERC20.sol tokens for testnet (DOT, USDT, USDC)"
    status: completed
    priority: P1
    estimate_hours: 2
    owner: claude
    blocked_by: [T001]
    tags: [smart-contract, must-have]
    notes: "Done. ERC20 with mint, approve, transfer, transferFrom"

  - id: T005
    subject: "Write Foundry tests for MockDEX and IntentExecutor"
    status: completed
    priority: P1
    estimate_hours: 4
    owner: claude
    blocked_by: [T003]
    tags: [smart-contract, testing, must-have]
    notes: "Done. 9/9 tests pass: swap, slippage, balance, zero, same token, preview, events, addLiquidity"

  - id: T006
    subject: "Write deploy script + seed pools script (Foundry)"
    status: completed
    priority: P1
    estimate_hours: 2
    owner: claude
    blocked_by: [T005]
    tags: [smart-contract, deploy, must-have]
    notes: "Done. Deploy.s.sol (tokens + DEX + executor) + SeedPools.s.sol (DOT/USDT + DOT/USDC pools)"

  - id: T007
    subject: "Build AI Intent Parser — LLM prompt + structured output"
    status: completed
    priority: P1
    estimate_hours: 5
    owner: claude
    blocked_by: []
    tags: [ai, must-have]
    notes: "Done. API route /api/parse-intent (OpenAI JSON mode). Validation logic extracted to intent-validator.ts (testable). 21 Jest tests pass (validateUserMessage: 8 tests, validateParsedIntent: 13 tests). ESLint clean. Chat.tsx wired to parser. TransactionPreview component ready. types.ts with shared types."

  - id: T008
    subject: "Build Risk Guardian module — slippage + liquidity scoring"
    status: completed
    priority: P1
    estimate_hours: 4
    owner: claude
    blocked_by: [T007]
    tags: [ai, risk, must-have]
    notes: "Done. risk-guardian.ts: getAmountOut (UniV2), calculatePriceImpact, assessRisk (GREEN/YELLOW/RED), formatTokenAmount. Thresholds: slippage >3%=YELLOW >10%=RED, liquidity <1000=YELLOW <100=RED, drain >10%=YELLOW >30%=RED. 22 new tests (risk-guardian.test.ts). All 43 tests pass, lint clean, build OK, check.sh 12/12."

  - id: T009
    subject: "Build Chat UI component — message list + input"
    status: completed
    priority: P1
    estimate_hours: 3
    owner: claude
    blocked_by: [T001]
    tags: [frontend, must-have]
    notes: "Done. Chat.tsx — messages list, input, loading state, stub parser ready for T007 wiring"

  - id: T010
    subject: "Build Wallet Connect component (wagmi + MetaMask)"
    status: completed
    priority: P1
    estimate_hours: 2
    owner: claude
    blocked_by: [T001]
    tags: [frontend, must-have]
    notes: "Done. ConnectWallet.tsx — connect/disconnect, address display, balance"

  - id: T011
    subject: "Build Transaction Preview + Confirm/Cancel component"
    status: completed
    priority: P1
    estimate_hours: 3
    owner: claude
    blocked_by: [T008, T009]
    tags: [frontend, must-have]
    notes: "Done. TransactionPreviewCard: swap summary (send/receive), slippage + price impact stats, risk badge (GREEN/YELLOW/RED with icons), confirm/cancel/blocked buttons, dismiss state, isExecuting loading. Chat.tsx wired to Risk Guardian via preview-builder.ts (buildPreview + getPoolReserves with seeded pool stubs). 18 new tests (preview-builder.test.ts). All 61 tests pass, lint clean, build OK."

  - id: T012
    subject: "Integrate full flow: Chat → Parser → Risk → Preview → Execute → Receipt"
    status: completed
    priority: P0
    estimate_hours: 6
    owner: claude
    blocked_by: [T003, T007, T008, T011]
    tags: [integration, must-have]
    notes: "Done. Full flow wired: Chat → /api/parse-intent → buildPreview (Risk Guardian) → TransactionPreviewCard → useSwapExecution (approve → executeSwap) → SwapReceiptCard. New files: abis.ts (MockERC20/MockDEX/IntentExecutor ABIs), useSwapExecution.ts (approve+execute hook with status states), usePoolReserves.ts (on-chain pool reads via wagmi), SwapReceipt.tsx (receipt card with tx hash + explorer link). preview-builder accepts on-chain pool override. Chat.tsx shows step-by-step status (approving/approved/executing), error handling (insufficient balance, rejected tx, reverted), 1% slippage tolerance. 12 new integration tests. All 73 tests pass, lint clean, build OK."

  - id: T013
    subject: "Deploy contracts to Polkadot Hub TestNet"
    status: completed
    priority: P1
    estimate_hours: 3
    owner: claude
    blocked_by: [T006]
    tags: [deploy, must-have]
    notes: "Done. Deployed to Polkadot Hub TestNet (chain 420420417, PAS tokens). Required: Foundry nightly + Solidity 0.8.19 + evm_version london + --legacy tx. Addresses: DOT=0x0Fb7..., USDT=0x12e4..., USDC=0x540D..., MockDEX=0x5b28..., IntentExecutor=0x13Da... Pools seeded: DOT/USDT 50K/337.5K, DOT/USDC 50K/337.5K. Frontend .env.local updated. wagmi config switched to polkadotHubTestnet. 73 tests pass, lint clean, build OK."

  - id: T014
    subject: "End-to-end testing on testnet (3 successful swaps)"
    status: completed
    priority: P0
    estimate_hours: 3
    owner: claude
    blocked_by: [T012, T013]
    tags: [testing, must-have]
    notes: "Done. 21 E2E tests (e2e-testnet.test.ts) on Polkadot Hub TestNet: 6 intent validation, 5 preview/risk, 4 on-chain reads, 3 on-chain swaps (DOT→USDT, DOT→USDC, USDT→DOT), 3 edge cases (slippage revert, zero amount, event emission). All 94 tests pass (73 frontend + 21 E2E). Also fixed: hydration error, Anthropic API support, wrong network detection + auto-switch, balance check in preview, improved UI."

  - id: T015
    subject: "Record demo video (1-3 min)"
    status: pending
    priority: P1
    estimate_hours: 3
    owner: unassigned
    blocked_by: [T014]
    tags: [submission, must-have]
    notes: "Show: connect wallet → type intent → risk score → confirm → success. Plus RED risk blocking."

  - id: T016
    subject: "Create pitch deck (5-7 slides)"
    status: pending
    priority: P1
    estimate_hours: 3
    owner: unassigned
    blocked_by: []
    tags: [submission, must-have]
    notes: "Problem → Solution → Demo → Architecture → Killer Features → Roadmap → Team"

  - id: T017
    subject: "Write GitHub README with setup instructions"
    status: completed
    priority: P2
    estimate_hours: 1
    owner: claude
    blocked_by: [T013]
    tags: [docs, must-have]
    notes: "Done. README.md with features, architecture, quick start, tech stack"

  - id: T018
    subject: "Submit to DoraHacks (repo + video + description)"
    status: pending
    priority: P0
    estimate_hours: 1
    owner: unassigned
    blocked_by: [T015, T016, T017]
    tags: [submission, must-have]
    notes: "Deadline: March 20. GitHub link + demo video + pitch deck"

  - id: T021
    subject: "Add quote expiry timer to Transaction Preview (like MetaMask)"
    status: completed
    priority: P1
    estimate_hours: 2
    owner: claude
    blocked_by: [T014]
    tags: [frontend, enhancement]
    notes: "Done. 30s countdown timer on preview card. When expired — quote auto-refreshes with new pool data. Visual: circular countdown + seconds. Expired state disables Confirm, shows 'Quote expired' + Refresh button."

  - id: T022
    subject: "Send tokens to address (transfer intent)"
    status: completed
    priority: P1
    estimate_hours: 4
    owner: claude
    blocked_by: [T023]
    tags: [frontend, smart-contract, must-have]
    notes: "Done. New 'transfer' intent type. executeTransfer in IntentExecutor (whitelist enforced). Updated types, validator, AI parser. New: useTransferExecution hook, TransferPreview/Receipt components, Chat.tsx branching by action. 20 new tests (114 total). Lint clean, build OK."

  - id: T023
    subject: "Token whitelist in IntentExecutor"
    status: completed
    priority: P1
    estimate_hours: 3
    owner: claude
    blocked_by: [T014]
    tags: [smart-contract, security, must-have]
    notes: "Done. whitelistedTokens mapping, onlyOwner/onlyWhitelisted modifiers, whitelistToken function, TokenWhitelisted event, factory role for auto-whitelist. Deploy script updated. 10 new Foundry tests (19 total). executeTransfer also added to contract."

  - id: T024
    subject: "Create token (ERC20 factory)"
    status: completed
    priority: P1
    estimate_hours: 4
    owner: claude
    blocked_by: [T023]
    tags: [smart-contract, frontend, must-have]
    notes: "Done. TokenFactory.sol: createToken (deploy+mint+auto-whitelist), getTokensByCreator, TokenCreated event. 7 Foundry tests. useTokenCreation hook, TokenCreatePreview/Receipt components. Updated types, validator (7 tests), parser, preview-builder (3 tests), Chat.tsx, contracts.ts, abis.ts, Deploy.s.sol. 26 Foundry + 114 Jest = 140 total tests. Lint clean, build OK."

  - id: T025
    subject: "NFT collections (roadmap only)"
    status: completed
    priority: P3
    estimate_hours: 0
    owner: claude
    blocked_by: []
    tags: [roadmap]
    notes: "Descoped to roadmap item for pitch deck. Not implementing for hackathon."

  - id: T026
    subject: "Landing page with scroll-triggered animations"
    status: completed
    priority: P1
    estimate_hours: 3
    owner: claude
    blocked_by: [T014]
    tags: [frontend, enhancement]
    notes: "Done. Full scrollable landing page: Hero (90vh with floating orbs, gradient text, demo card), Features (2x3 grid with 6 glass cards), How It Works (3-step horizontal), Roadmap (v2/v3 phases), CTA section, enhanced footer. CSS animations: fadeInUp, float, pulseGlow. IntersectionObserver-based scroll animations. Sticky header. All responsive. 121 tests pass, build clean."

  # --- Nice-to-have (do NOT start until all above completed) ---

  # ============================================================
  # PVM TRACK — XCM Cross-Chain + Rust Risk Engine
  # Branch: feature/pvm-track
  # Goal: Submit IntentDOT to PVM Smart Contracts track
  # Categories: "Call Rust from Solidity" + "Precompiles (XCM)"
  # ============================================================

  # --- Phase 1: XCM Cross-Chain Transfers ---

  - id: T030
    subject: "Research XCM precompile: SCALE encoding, message format, testnet verification"
    status: pending
    priority: P0
    estimate_hours: 3
    owner: unassigned
    blocked_by: []
    tags: [research, xcm, pvm-track]
    notes: "Read docs, build valid XCM message, test weighMessage on testnet via cast. Determine destination chains available on Paseo testnet."

  - id: T031
    subject: "Write XCMBridge.sol — Solidity wrapper over XCM precompile (0xA0000)"
    status: pending
    priority: P0
    estimate_hours: 5
    owner: unassigned
    blocked_by: [T030]
    tags: [smart-contract, xcm, pvm-track]
    notes: "Contract that encodes XCM messages and calls precompile. Functions: bridgeAsset(destParaId, beneficiary, token, amount). SCALE encoding for WithdrawAsset + BuyExecution + DepositAsset."

  - id: T032
    subject: "Write Foundry tests for XCMBridge"
    status: pending
    priority: P0
    estimate_hours: 3
    owner: unassigned
    blocked_by: [T031]
    tags: [testing, xcm, pvm-track]
    notes: "Test message encoding, parameter validation, revert cases. May need fork-test against testnet for precompile calls."

  - id: T033
    subject: "Deploy XCMBridge to Polkadot Hub TestNet"
    status: pending
    priority: P0
    estimate_hours: 2
    owner: unassigned
    blocked_by: [T032]
    tags: [deploy, xcm, pvm-track]
    notes: "Deploy, verify on Blockscout, test bridgeAsset with real XCM call."

  - id: T034
    subject: "Add 'bridge' intent type to AI parser + validator"
    status: pending
    priority: P0
    estimate_hours: 3
    owner: unassigned
    blocked_by: [T031]
    tags: [ai, frontend, xcm, pvm-track]
    notes: "New action: bridge. Fields: token, amount, destination_chain, recipient. Update parse-intent API, intent-validator.ts, types.ts."

  - id: T035
    subject: "Build BridgePreview + BridgeReceipt components"
    status: pending
    priority: P1
    estimate_hours: 3
    owner: unassigned
    blocked_by: [T034]
    tags: [frontend, xcm, pvm-track]
    notes: "Preview: show destination chain, amount, estimated fees. Receipt: tx hash, explorer link, XCM status."

  - id: T036
    subject: "Build useBridgeExecution hook + integrate in Chat"
    status: pending
    priority: P1
    estimate_hours: 3
    owner: unassigned
    blocked_by: [T033, T035]
    tags: [frontend, xcm, pvm-track]
    notes: "Hook: approve → bridge call → receipt. Wire into Chat.tsx handleBridgeIntent + handleBridgeConfirm."

  - id: T037
    subject: "Write Jest tests for bridge intent parsing + preview"
    status: pending
    priority: P1
    estimate_hours: 2
    owner: unassigned
    blocked_by: [T034, T035]
    tags: [testing, xcm, pvm-track]
    notes: "Validator tests for bridge intent, preview builder tests."

  # --- Phase 2: Rust Risk Engine ---

  - id: T040
    subject: "Scaffold Rust PVM contract from flipper template"
    status: completed
    priority: P0
    estimate_hours: 2
    owner: unassigned
    blocked_by: []
    tags: [rust, pvm-track]
    notes: "Clone template, set up no_std project, configure for polkavm target. Verify build toolchain works."

  - id: T041
    subject: "Implement RiskEngine.rs — price impact + MA20 + volatility scoring"
    status: completed
    priority: P0
    estimate_hours: 5
    owner: unassigned
    blocked_by: [T040]
    tags: [rust, smart-contract, pvm-track]
    notes: "Functions: evaluate(amountIn, reserveIn, reserveOut) → (riskLevel, score, priceImpact, volatility). Storage: ring buffer of 20 prices, MA20, stddev. Integer arithmetic only. Selectors compatible with Solidity interface."

  - id: T042
    subject: "Build + deploy RiskEngine to Polkadot Hub TestNet"
    status: completed
    priority: P0
    estimate_hours: 3
    owner: claude
    blocked_by: [T041]
    tags: [deploy, rust, pvm-track]
    notes: "Deployed at 0x33B154E3fE00e4e9733c1C6ca771E7Ae24B091E9. Key fix: polkatool 0.31→0.26 version mismatch caused CodeRejected. evaluate() and getStats() verified working via cast call."

  - id: T043
    subject: "Add IRiskEngine interface + integrate into IntentExecutor.sol"
    status: completed
    priority: P0
    estimate_hours: 3
    owner: claude
    blocked_by: [T042]
    tags: [smart-contract, rust, pvm-track]
    notes: "IRiskEngine.sol interface. IntentExecutor: setRiskEngine(), evaluate() before swap, RED→revert. RiskChecked event emitted. 11 tests added (T044 done together)."

  - id: T044
    subject: "Write Foundry tests for RiskEngine integration"
    status: completed
    priority: P1
    estimate_hours: 3
    owner: claude
    blocked_by: [T043]
    tags: [testing, rust, pvm-track]
    notes: "11 tests in RiskEngine.t.sol: GREEN/YELLOW pass, RED reverts, events, reserves, disable, access control. All 37 tests pass."

  - id: T045
    subject: "Show on-chain risk verification badge after swap"
    status: completed
    priority: P1
    estimate_hours: 2
    owner: claude
    blocked_by: [T043]
    tags: [frontend, rust, pvm-track]
    notes: "Done. RiskChecked event parsed from receipt. Badge shows GREEN/YELLOW/RED with score, impact, volatility. Tooltip explains how it works. Extracted risk-display.ts utility (getRiskLabel, getRiskColors, formatBps, parseRiskCheckedEvent)."

  - id: T046
    subject: "Write Jest tests for on-chain risk display"
    status: completed
    priority: P1
    estimate_hours: 1
    owner: claude
    blocked_by: [T045]
    tags: [testing, rust, pvm-track]
    notes: "Done. 28 tests in risk-display.test.ts: getRiskLabel (6), getRiskColors (4), formatBps (6), parseRiskCheckedEvent with pre-encoded event logs (8), display integration (4). All 128 Jest tests pass."

  # --- Phase 3: Docs + Submission ---

  - id: T050
    subject: "Update ARCHITECTURE.md, README, SECURITY.md for PVM features"
    status: completed
    priority: P1
    estimate_hours: 2
    owner: claude
    blocked_by: [T045]
    tags: [docs, pvm-track]
    notes: "Done. README: added On-Chain Risk Engine section (why, how, two-layer, per-pool, tech), updated architecture diagram, test counts. ARCHITECTURE: added RiskEngine component, Rust PVM tech stack row, two-layer risk in data flow. SECURITY: added on-chain risk checks, per-pool isolation. TESTPLAN: added 11 RiskEngine tests, 28 risk-display tests, updated totals (37 Foundry + 128 Jest)."

  - id: T051
    subject: "Record demo video showing XCM bridge + Rust risk blocking"
    status: pending
    priority: P0
    estimate_hours: 3
    owner: unassigned
    blocked_by: [T036, T045]
    tags: [submission, pvm-track]
    notes: "Demo: swap with GREEN risk → success. Large swap → RED → Rust blocks on-chain. Bridge DOT to parachain → XCM success."

  - id: T052
    subject: "Submit to DoraHacks PVM track"
    status: pending
    priority: P0
    estimate_hours: 1
    owner: unassigned
    blocked_by: [T050, T051]
    tags: [submission, pvm-track]
    notes: "Separate submission for PVM Smart Contracts track. Link same repo, highlight PVM features."

  # --- Deferred (not for hackathon) ---

  - id: T020
    subject: "Add conditional intents (DCA / stop-loss keeper)"
    status: pending
    priority: P3
    estimate_hours: 10
    owner: unassigned
    blocked_by: [T014]
    tags: [smart-contract, nice-to-have]
    notes: "ONLY if time permits. Show as roadmap item in pitch."
